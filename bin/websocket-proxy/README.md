# `websocket-proxy`

WebSocket fan-out proxy for the Base Stack.

Subscribes to one or more upstream WebSocket servers and broadcasts messages to all connected downstream clients, with optional authentication, rate limiting, and brotli compression.
