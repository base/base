# `base-proposer-bin`

TEE-based output proposer binary for Base chains.

Parses CLI arguments, builds a validated configuration, and delegates to
`base_proposer::run()` for the full service lifecycle.
