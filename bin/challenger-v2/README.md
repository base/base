# `base-challenger-v2-bin`

Fault-proof challenger binary for Base.

Parses CLI arguments, builds a validated configuration, and delegates to
`base_challenger_v2::ChallengerService::run()` for the full service lifecycle.
