# `base-challenger-bin`

ZK-proof dispute game challenger binary for Base chains.

Parses CLI arguments, builds a validated configuration, and delegates to
`base_challenger::run()` for the full service lifecycle.
