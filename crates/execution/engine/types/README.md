# Execution engine types

Shared direct-engine commands, fork-choice state, execution events, and observer notifications.

The `serde-bincode-compat` feature preserves the observer write-ahead-log encoding.
