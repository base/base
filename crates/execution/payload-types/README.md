# base-execution-payload-types

Base payload attributes, built payloads, Engine API validation, errors, and event types.

This crate contains the data shared by execution, RPC, and payload construction.
Payload job scheduling and builder services live in `base-execution-payload-builder`.
