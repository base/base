# Base Prover Service Client

`base-prover-service-client` provides role-specific JSON-RPC clients for the
Base prover service.

The crate depends on `base-prover-service-protocol` for the wire contract and
exposes separate requester and worker client types.
