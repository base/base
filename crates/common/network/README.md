# Base transaction building and signing

Shared transaction builders, wallets, and local and remote signers for Base and its Ethereum L1 connections. Base RPC request types and their network mapping live in `base-common-types-rpc`.
