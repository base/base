# Base state

Shared database interfaces, account state, execution caches, bundle transitions, and rollback for Base execution and proofs. Supports `no_std`; the optional `alloydb` feature provides an RPC-backed database.
