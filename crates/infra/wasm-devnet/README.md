# base-wasm-devnet

In-browser devnet: L1 + batcher + sequencer (CL/EL) + validator (CL/EL) compiled to WebAssembly.

Requires `SharedArrayBuffer` (COOP/COEP headers) for multi-threaded operation in Chrome.
