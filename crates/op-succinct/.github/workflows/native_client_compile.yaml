name: Compile Native Programs

on:
  pull_request:
    branches:
      - main

jobs:
  build_and_run:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3
    - name: Compile the Validity Client program
      run: cargo build --profile release-client-lto
      working-directory: validity-client
    - name: Compile the zkVM Client program
      run: cargo build --profile release-client-lto
      working-directory: zkvm-client
