# `base-system-tests`

System test infrastructure for Base node.

This crate provides a programmatic way to spin up isolated L1+L2 stacks for
system tests using testcontainers for container orchestration.
