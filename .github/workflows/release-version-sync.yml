name: Release Version Sync

on:
  create:

permissions:
  contents: write
  pull-requests: write
  issues: write

jobs:
  sync-version:
    # Only run for release branches
    if: github.ref_type == 'branch' && startsWith(github.ref_name, 'releases/v')
    runs-on: ubuntu-latest

    steps:
      - name: Harden the runner (Audit all outbound calls)
        uses: step-security/harden-runner@002fdce3c6a235733a90a27c80493a3241e56863 # v2.12.1
        with:
          egress-policy: audit

      - name: Checkout release branch
        uses: actions/checkout@08c6903cd8c0fde910a37f88322edcfb5dd907a8 # v5.0.0
        with:
          ref: ${{ github.ref_name }}

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@4be9e76fd7c4901c61fb841f559994984270fce7 # stable

      - name: Run version sync
        env:
          GH_TOKEN: ${{ github.token }}
        run: ./scripts/release/version-sync.sh "${{ github.ref_name }}"

      - name: Create release tracking issue
        run: ./scripts/release/create-issue.sh "${{ github.ref_name }}"
