# Contracts

This page will specify the proof contracts.

The proof contracts verify proof material, track proposal and challenge state, enforce finality
windows, and release withdrawals and bonds according to the game result. The full contracts
specification will define `AggregateVerifier`, proof-specific verifier contracts, registry
contracts, `DelayedWETH`, and their interactions with the portal and anchor state registry.
