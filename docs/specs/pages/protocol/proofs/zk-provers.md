# ZK Provers

This page will specify the ZK prover component.

ZK provers produce permissionless proof material for checkpoint proposals and disputes. The full ZK
prover specification will define request formats, proof modes, receipt encoding, proving-service
status transitions, retry behavior, and onchain verifier expectations.
