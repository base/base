schema_version = 1
id = "system-contract-activation-registry-admin-lifecycle"
description = "External RPC contract acceptance for activation_registry_admin_lifecycle"
timeout = "5m"

[ci]
suite = "extended"

[devnet.l2.forks]
azul = { at_block = 0 }
beryl = { at_block = 1 }
cobalt = { disabled = true }
denim = { disabled = true }
zenith = { disabled = true }

[[checks]]
id = "activation-registry-admin-lifecycle"
kind = "contract"
case = "activation_registry_admin_lifecycle"
timeout = "3m"
start = { after_fork = "beryl", chain = "l2" }
