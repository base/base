schema_version = 1
id = "system-contract-activation-registry-check-activated-gate"
description = "External RPC contract acceptance for activation_registry_check_activated_gate"
timeout = "5m"

[ci]
suite = "extended"

[devnet.l2.forks]
azul = { at_block = 0 }
beryl = { at_block = 1 }
cobalt = { disabled = true }
denim = { disabled = true }
zenith = { disabled = true }

[[checks]]
id = "activation-registry-check-activated-gate"
kind = "contract"
case = "activation_registry_check_activated_gate"
timeout = "3m"
start = { after_fork = "beryl", chain = "l2" }
