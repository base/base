schema_version = 1
id = "system-contract-activation-registry-is-activated-default"
description = "External RPC contract acceptance for activation_registry_is_activated_default"
timeout = "5m"

[ci]
suite = "pr"

[devnet.l2.forks]
azul = { at_block = 0 }
beryl = { at_block = 1 }
cobalt = { disabled = true }
denim = { disabled = true }
zenith = { disabled = true }

[[checks]]
id = "activation-registry-is-activated-default"
kind = "contract"
case = "activation_registry_is_activated_default"
timeout = "3m"
start = { after_fork = "beryl", chain = "l2" }
