schema_version = 1
id = "system-contract-policy-registry-policy-exists"
description = "External RPC contract acceptance for policy_registry_policy_exists"
timeout = "5m"

[ci]
suite = "extended"

[devnet.l2.forks]
azul = { at_block = 0 }
beryl = { at_block = 1 }
cobalt = { disabled = true }
denim = { disabled = true }
zenith = { disabled = true }

[[checks]]
id = "policy-registry-policy-exists"
kind = "contract"
case = "policy_registry_policy_exists"
timeout = "3m"
start = { after_fork = "beryl", chain = "l2" }
