# RPC conversion

Transaction and block response conversion, RPC call fees, state overrides, and EIP-8130 simulation
request handling. These adapters construct execution inputs without making the EVM runtime depend
on RPC request schemas.
